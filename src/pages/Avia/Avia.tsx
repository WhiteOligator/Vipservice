import { Layout } from "../../components/Layout";
import { DataWidget } from "../../components/DataWidget/DataWidget";

export const Avia = () => {
  return (
    <Layout>
      <DataWidget />
    </Layout>
  );
};
