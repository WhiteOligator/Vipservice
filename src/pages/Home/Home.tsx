import { HomeWidget } from '../../components/HomeWidget/HomeWidget'
import { Layout } from '../../components/Layout'

export const Home = () => {
  return (
    <Layout>
        <HomeWidget />
    </Layout>
  )
}
